

export const checkNotif = async (userid) => {
  try {
    // const data = await getAll("Crypto");
    // return data;
  } catch (err) {
    throw new Error(err.message);
  }
};

export const isLue = async (userid) => {
    try {
        
    } catch (err) {
        throw new Error(err.message);
    }
}